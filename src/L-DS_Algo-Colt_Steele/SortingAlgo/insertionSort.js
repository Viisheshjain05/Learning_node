// ? insertion Sorting Algo

// insertion sort Check the next value and fit it in the right place where it has to be
// EX- its like hand picking value from start and placing them in right order before the selected position

/**
 * ? Use Case
 *  best for small arrays of data
 *  Performance Complexity- O(nlog2)
 *  Space Complexity - O(1)
 */

// * FileName           -   insertionSort
// * Contain properties - [ Sorting ];

const init = () => {
  const insertionSort_naive = (arr) => {
    return arr;
  
  };

  // ! SOL -
  console.log(insertionSort_naive([3, 2, 1, 4, 5, 6, 7, 9, 8, 10]));
};

module.exports = init;
